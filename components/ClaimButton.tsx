import React from "react"
import { Button } from "@web3uikit/core"

const ClaimButton = () => {
    return (
        <div>
            <Button text="Claim"></Button>
        </div>
    )
}

export default ClaimButton
